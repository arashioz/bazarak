services:
  bazarek:
    build:
      context: ./web_market
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      DATABASE_PATH: /data/database.json
    volumes:
      - bazarek-data:/data
    restart: unless-stopped

volumes:
  bazarek-data:
