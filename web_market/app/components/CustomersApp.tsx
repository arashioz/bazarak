"use client";

import { Phone, Search, Settings2, StickyNote, Users, X } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import customers from "../data/customers.json";

type Customer = (typeof customers)[number];
type CustomerNote = { id: number; text: string; updatedAt: string };
type CustomerSettings = { categories: { id: string; name: string }[]; assignments: Record<number, string> };
const STORE = "bazarek-customer-notes";

const digits = (value: string) => value.replace(/\D/g, "");
const callNumber = (customer: Customer) => customer.mobile || customer.phone;

export default function CustomersApp() {
  const [query, setQuery] = useState("");
  const [notes, setNotes] = useState<Record<number, CustomerNote>>({});
  const [selected, setSelected] = useState<Customer | null>(null);
  const [customerSettings, setCustomerSettings] = useState<CustomerSettings>({ categories: [], assignments: {} });
  const [categoryFilter, setCategoryFilter] = useState(""); const [showSettings, setShowSettings] = useState(false);
  useEffect(() => { fetch("/api/database").then((response) => response.json()).then((database) => { setNotes(database.customerNotes || {}); setCustomerSettings(database.customerSettings || { categories: [], assignments: {} }); }).catch(() => { try { setNotes(JSON.parse(localStorage.getItem(STORE) || "{}")); } catch {} }); }, []);
  const saveCustomerSettings = (next: CustomerSettings) => { setCustomerSettings(next); void fetch("/api/database", { method: "PUT", headers: { "content-type": "application/json" }, body: JSON.stringify({ section: "customerSettings", data: next }) }); };
  const saveNote = (id: number, text: string) => { const next = { ...notes, [id]: { id, text, updatedAt: new Date().toISOString() } }; setNotes(next); localStorage.setItem(STORE, JSON.stringify(next)); void fetch("/api/database", { method: "PUT", headers: { "content-type": "application/json" }, body: JSON.stringify({ section: "customerNotes", data: next }) }); };
  const list = useMemo(() => { const term = query.trim(); return customers.filter((customer) => (!term || `${customer.name} ${customer.group} ${customer.mobile} ${customer.phone}`.includes(term)) && (!categoryFilter || customerSettings.assignments[customer.id] === categoryFilter)); }, [query, categoryFilter, customerSettings]);
  return <main className="min-h-screen bg-blush p-4 text-oxblood-dark sm:p-6"><section className="mx-auto max-w-6xl"><header className="flex flex-wrap items-center justify-between gap-3"><div><h1 className="flex items-center gap-2 text-2xl font-black text-oxblood"><Users /> مشتریان</h1><p className="mt-1 text-sm opacity-60">{customers.length.toLocaleString("fa-IR")} شخص</p></div><div className="flex gap-2"><button onClick={() => setShowSettings(true)} className="rounded-lg border border-oxblood/20 bg-white px-4 py-2 text-sm font-bold text-oxblood"><Settings2 className="inline" size={16}/> تنظیمات دسته</button><a href="/modir/panel" className="rounded-lg border border-oxblood/20 bg-white px-4 py-2 text-sm font-bold text-oxblood">پنل مدیریت</a></div></header><div className="mt-5 grid gap-2 sm:grid-cols-[1fr_220px]"><div className="relative"><Search className="absolute right-3 top-3 text-oxblood/45" size={18}/><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="جستجو نام، گروه یا شماره تماس" className="w-full rounded-lg border border-oxblood/15 bg-white py-3 pr-10 pl-3"/></div><select value={categoryFilter} onChange={(event) => setCategoryFilter(event.target.value)} className="rounded-lg border border-oxblood/15 bg-white p-3"><option value="">همه دسته‌ها</option>{customerSettings.categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{list.map((customer) => { const number = callNumber(customer); const category = customerSettings.categories.find((item) => item.id === customerSettings.assignments[customer.id]); return <article key={customer.id} className="rounded-lg border border-oxblood/10 bg-white p-4 shadow-sm"><button onClick={() => setSelected(customer)} className="w-full text-right"><b className="block text-base">{customer.name}</b><small className="mt-1 block opacity-55">{category?.name || customer.group || "بدون گروه"}</small>{number && <strong dir="ltr" className="mt-3 block text-right text-oxblood">{number}</strong>}</button><div className="mt-3 flex gap-2"><a href={number ? `tel:${digits(number)}` : undefined} className={`inline-flex items-center gap-1 rounded-lg px-3 py-2 text-sm font-bold ${number ? "bg-oxblood text-white" : "cursor-not-allowed bg-gray-100 text-gray-400"}`}><Phone size={16}/> تماس</a><button onClick={() => setSelected(customer)} className="rounded-lg border border-oxblood/20 px-3 py-2 text-sm font-bold text-oxblood">ویرایش</button></div></article>; })}</div></section>{showSettings && <CustomerCategorySettings settings={customerSettings} onClose={() => setShowSettings(false)} onSave={saveCustomerSettings}/>} {selected && <CustomerDetail customer={selected} note={notes[selected.id]?.text || ""} categories={customerSettings.categories} categoryId={customerSettings.assignments[selected.id] || ""} onCategory={(categoryId) => saveCustomerSettings({ ...customerSettings, assignments: { ...customerSettings.assignments, [selected.id]: categoryId } })} onClose={() => setSelected(null)} onSave={saveNote}/>}</main>;
}

function CustomerCategorySettings({ settings, onClose, onSave }: { settings: CustomerSettings; onClose: () => void; onSave: (settings: CustomerSettings) => void }) { const [name, setName] = useState(""); return <div className="fixed inset-0 z-20 flex items-end justify-center"><button onClick={onClose} className="absolute inset-0 bg-oxblood/40"/><section className="relative w-full rounded-t-2xl bg-white p-5 shadow-2xl sm:max-w-2xl"><button onClick={onClose} className="absolute left-4 top-4"><X/></button><h2 className="text-xl font-black">دسته‌بندی مشتریان</h2><form onSubmit={(event) => { event.preventDefault(); if (name.trim()) { onSave({ ...settings, categories: [...settings.categories, { id: `${Date.now()}`, name: name.trim() }] }); setName(""); } }} className="mt-4 flex gap-2"><input value={name} onChange={(event) => setName(event.target.value)} placeholder="نام دسته جدید" className="flex-1 rounded border p-2"/><button className="rounded bg-oxblood px-4 text-white">افزودن</button></form>{settings.categories.map((category) => <div key={category.id} className="mt-2 flex justify-between rounded border p-2"><span>{category.name}</span><button onClick={() => onSave({ ...settings, categories: settings.categories.filter((item) => item.id !== category.id) })} className="text-oxblood">حذف</button></div>)}</section></div>; }

function CustomerDetail({ customer, note, categories, categoryId, onCategory, onClose, onSave }: { customer: Customer; note: string; categories: CustomerSettings["categories"]; categoryId: string; onCategory: (id: string) => void; onClose: () => void; onSave: (id: number, text: string) => void }) {
  const [text, setText] = useState(note); const number = callNumber(customer);
  return <div className="fixed inset-0 z-20 flex items-end justify-center"><button onClick={onClose} className="absolute inset-0 bg-oxblood/40"/><section className="relative w-full rounded-t-2xl bg-white p-5 shadow-2xl sm:max-w-2xl"><button onClick={onClose} className="absolute left-4 top-4 text-oxblood"><X/></button><h2 className="text-xl font-black">{customer.name}</h2><p className="mt-2 text-sm opacity-60">{customer.group}</p>{number && <a href={`tel:${digits(number)}`} dir="ltr" className="mt-4 inline-flex items-center gap-2 rounded-lg bg-oxblood px-4 py-2 font-bold text-white"><Phone size={17}/> {number}</a>}<label className="mt-5 block text-sm font-bold">دسته مشتری<select value={categoryId} onChange={(event) => onCategory(event.target.value)} className="mt-2 w-full rounded-lg border p-3"><option value="">بدون دسته</option>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select></label><label className="mt-5 block text-sm font-bold">یادداشت داخلی<textarea value={text} onChange={(event) => setText(event.target.value)} rows={4} placeholder="مثلاً زمان مناسب تماس یا توضیح سفارش..." className="mt-2 w-full rounded-lg border border-oxblood/15 p-3 font-normal"/></label><button onClick={() => { onSave(customer.id, text.trim()); onClose(); }} className="mt-3 w-full rounded-lg bg-oxblood p-3 font-bold text-white">ذخیره یادداشت</button></section></div>;
}
