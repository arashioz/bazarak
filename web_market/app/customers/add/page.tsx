"use client";
import Link from "next/link";
import { FormEvent, useState } from "react";
export default function AddCustomerPage(){const[n,setN]=useState(""),[p,setP]=useState(""),[a,setA]=useState("");const save=async(e:FormEvent)=>{e.preventDefault();if(!n.trim())return;const customer={id:Date.now(),name:n.trim(),mobile:p.trim(),phone:"",address:a.trim(),group:"",description:"",active:true,followUps:[]};await fetch("/api/database",{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({section:"customerUpsert",data:customer})});location.href="/customers"};return <main className="min-h-screen bg-blush p-5"><form onSubmit={save} className="mx-auto max-w-md rounded-2xl bg-white p-5 shadow"><h1 className="text-xl font-black">افزودن مشتری</h1><input required value={n} onChange={e=>setN(e.target.value)} placeholder="نام مشتری" className="mt-4 w-full rounded border p-3"/><input value={p} onChange={e=>setP(e.target.value)} placeholder="شماره تماس" className="mt-3 w-full rounded border p-3"/><input value={a} onChange={e=>setA(e.target.value)} placeholder="آدرس" className="mt-3 w-full rounded border p-3"/><button className="mt-4 w-full rounded bg-oxblood p-3 font-bold text-white">ذخیره مشتری</button><Link href="/customers" className="mt-3 block text-center">انصراف</Link></form></main>}
